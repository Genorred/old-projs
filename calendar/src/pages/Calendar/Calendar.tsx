import React from 'react';
import Button from "../../UI/Button/Button";

const Calendar = () => {
    return (
        <div>
            <Button>j</Button>
        </div>
    );
};

export default Calendar;